# Tests that deliberately do not run, and why. Checked by scripts/test-hygiene.sh.
#
#   <path> <construct> <count> <reason>
#
# `construct` is `disabled` or `withKnownIssue`; `count` is the exact number of
# sites in that file. An entry that outlives its construct fails the gate, so
# the list cannot quietly outlast the reason for it.
#
# Nothing belongs here to park a failure. A test that fails is a defect to fix
# or a test to delete.

Tests/GlasstualTests/IsolationSupportTests.swift withKnownIssue 2 The isolation helpers are the subject: each of these proves that expectMainActor()/expectOffMainActor() records an issue when the caller is on the wrong actor, so the failure is the assertion.
